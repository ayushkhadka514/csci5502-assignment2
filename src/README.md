# csci5502-assignment2
Assignment 2 CSCI 5502
Ayush Khadka and Kaylee Dehncke
## Steps to Run

1. **Install Dependencies**:
   ```bash
   pip install -r src/requirements.txt
   ```

2. **Run the Notebook**:
   Open the `dataset_insight.ipynb` file in Jupyter Notebook or JupyterLab and execute the cells.

3. **Data Files**:
   Ensure the `data/` folder contains the required CSV files (`student.csv` and `winequality-red.csv`).

4. **Logs**:
   Any logs generated will be saved in the `logs/` folder.
